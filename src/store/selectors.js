export const selectList = (state) => {
  return state.list;
};

export const selectTextValue = (state) => {
  return state.textValue;
};

export const selectModalIsVisible = (state) => {
  return state.modalIsVisible;
};

export const selectModalText = (state) => {
  return state.modalText;
};

export const selectCurrentKey = (state) => {
  return state.currentKey;
};
